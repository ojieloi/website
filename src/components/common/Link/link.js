import styled from "styled-components";

const Link = styled.a`
  color: #000;
  font-family: "DM Sans", sans-serif;
  font-size: 16px;
  font-weight: 700;
  text-decoration: underline;
`;

export default Link;
